// @ts-check

/**
 * Sidebar for the All docs... plugin instance
 */
const sidebars = {
  // Default sidebar for the all-docs plugin
  sidebar: [
    'sample1',
    'sample2',
  ],
};

export default sidebars;
